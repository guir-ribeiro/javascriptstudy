let heroes = ['Batman', 'Catwoman', 'Iron Man'];
const ages = [31, 25, 39, 40, 25];
const randomArray = ['Parker', 'Diana', 19, 18]

// Métodos Arrays

const joinHeroes = heroes.join('-'); // faz um copia e retorna os valores separado pelo elemento adicionado 
const indexOf25 = ages.indexOf(25); // retorna o index do elemento selecionado
const moreHeroes = heroes.concat(['Superman', 'Wolverine']); // faz uma copia do array e cria igual adicionando os elementos concatenados

const pushHeroes = heroes.push('Cyclops', 'Hulk'); // add ao array e retona a quantidade de elementos
const popHeroes = heroes.pop() // Tira o ultimo item do array e retorna o valor ]


let emptiness = null
// console.log(emptiness)

const names = ['dio', 'roger', 'robert']

const arrayIncludes = names.includes('Guilherme') // Verifica se tem esse argumento dentro do meu array

// console.log(arrayIncludes)
